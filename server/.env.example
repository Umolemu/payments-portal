# --------------------------------------------------------------------
# 🌐 Server Configuration
# --------------------------------------------------------------------

# Port your HTTPS server runs on
PORT=5001

# Absolute or relative paths to your SSL certificate and key
SSL_KEY_PATH=keys/privateKey.pem
SSL_CERT_PATH=keys/certificate.pem

# Frontend origin allowed by CORS (adjust this for your frontend URL)
FRONTEND_ORIGIN=https://localhost:3000

# Node environment mode (development | production)
NODE_ENV=development

# --------------------------------------------------------------------
# 🔑 Authentication / Security
# --------------------------------------------------------------------

# Example secret for JWTs or other token signing
JWT_SECRET=your-super-secure-random-secret
ACCESS_TOKEN_EXPIRES_IN=15m

# Number of bcrypt salt rounds for password hashing
SALT_ROUNDS=10
